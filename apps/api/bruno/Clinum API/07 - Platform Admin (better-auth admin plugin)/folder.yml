info:
  name: 07 - Platform Admin (better-auth admin plugin)
  type: folder
  seq: 8

request:
  auth: inherit

docs:
  content: Endpoints nativos do plugin `admin` do better-auth, em /api/auth/admin/*. Gate próprio do plugin (adminRoles = ["super_admin"], ver auth.ts) — INDEPENDENTE do PlatformAdminGuard da pasta acima. Todos usam {{cookie_superadmin}} e mandam Origin por padrão.
  type: text/markdown
