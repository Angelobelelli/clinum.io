info:
  name: 01 - Auth (better-auth)
  type: folder
  seq: 2

request:
  auth: inherit

docs:
  content: Rotas base do better-auth, montadas em /api/auth/* (ver mount-auth.ts). Todos os requests já mandam Origin por padrão (ver nota no topo da collection).
  type: text/markdown
