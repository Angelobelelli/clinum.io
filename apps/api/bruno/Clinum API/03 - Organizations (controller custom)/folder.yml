info:
  name: 03 - Organizations (controller custom)
  type: folder
  seq: 4

request:
  auth: inherit

docs:
  content: OrganizationsController (apps/api/src/organizations/) — rotas de teste manual da fundação de tenant, sem passar pelo plugin organization do better-auth. Não exigem Origin (não são /api/auth/*) nem sessão.
  type: text/markdown
