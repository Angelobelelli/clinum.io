info:
  name: 05 - Patients
  type: folder
  seq: 6

request:
  auth: inherit

docs:
  content: PatientsController (apps/api/src/modules/patients/) — primeiro model de negócio real, com as duas camadas de isolamento de tenant (aplicação + RLS) ativas. owner/admin/staff têm acesso total (inclusive ficha de saúde); reception cria/lê/atualiza só os dados básicos, NUNCA a ficha de saúde. Todos os requests aqui precisam de X-Tenant-Slug.
  type: text/markdown
