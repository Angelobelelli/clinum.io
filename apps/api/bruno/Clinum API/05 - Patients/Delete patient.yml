info:
  name: Delete patient
  type: http
  seq: 5

http:
  method: DELETE
  url: '{{baseUrl}}/patients/{{testPatientId}}'
  headers:
    - name: X-Tenant-Slug
      value: '{{tenantSlug1}}'
    - name: Cookie
      value: '{{cookie_owner1}}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
