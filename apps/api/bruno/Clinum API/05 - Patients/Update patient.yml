info:
  name: Update patient
  type: http
  seq: 4

http:
  method: PATCH
  url: '{{baseUrl}}/patients/{{testPatientId}}'
  headers:
    - name: Content-Type
      value: application/json
    - name: X-Tenant-Slug
      value: '{{tenantSlug1}}'
    - name: Cookie
      value: '{{cookie_owner1}}'
  body:
    type: json
    data: |-
      {
        "telefone": "11900001111"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Todos os campos são opcionais aqui (partial do create).
