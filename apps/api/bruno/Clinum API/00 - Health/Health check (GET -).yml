info:
  name: Health check (GET /)
  type: http
  seq: 1

http:
  method: GET
  url: '{{baseUrl}}/'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Rota raiz do AppController — não passa pelo TenantMiddleware (excluída explicitamente em app.module.ts).
